# Contributing

Orientation uses small, architecture-aligned slices.

Before changing cross-context ownership, published boundaries, renderer semantics, routing/provider boundaries, location privacy rules, or runtime topology, read `AGENTS.md` and the relevant documents under `docs/`.

Normal development targets `dev`. Keep commits focused and tests close to the changed capability.
