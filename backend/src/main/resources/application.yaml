spring:
  application:
    name: orientation

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  shutdown: graceful

orientation:
  photon:
    base-url: https://photon.komoot.io
    connect-timeout: 3s
    read-timeout: 5s
    user-agent: wgt-system-orientation/0.2.0
