package system.wgt.orientation.application.research;

public record GeneratedSpatialResearchPrompt(
        String contract,
        String version,
        String schemaId,
        String prompt) {
}
