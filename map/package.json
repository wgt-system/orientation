{
  "name": "@wgt-system/orientation-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "maplibre-gl": "6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
